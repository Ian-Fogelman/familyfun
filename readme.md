# Tell Flask where your application is
export FLASK_APP=hello.py 
# On Windows use `set FLASK_APP=hello.py`

# Run the development server
flask run

```
flask --app app_name run --debug --host=0.0.0.0
```

http://127.0.0.1:5000